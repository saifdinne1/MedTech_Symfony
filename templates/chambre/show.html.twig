{% extends 'dashboard.html.twig' %} 

{% block title %}Chambre{% endblock %}

{% block contenu %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chambre.id }}</td>
            </tr>
            <tr>
                <th>Numero_chambre</th>
                <td>{{ chambre.numeroChambre }}</td>
            </tr>
            <tr>
                <th>service</th>
                <td>{{ chambre.serviceAffecter }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chambre_index') }}">back to list</a>

    <a href="{{ path('app_chambre_edit', {'id': chambre.id}) }}">edit</a>

    <a class="btn btn-qrcode" href="{{ path('qr_code',{'id' : chambre.id}) }}">QR Code</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}

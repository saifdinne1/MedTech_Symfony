{% extends 'dashboard.html.twig' %} 

{% block title %}Facture{% endblock %}

{% block contenu %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>nom du patient</th>
                <td>{{ facture.numeroFacture }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ facture.designation }}</td>
            </tr>
            <tr>
                <th>Prix_designation</th>
                <td>{{ facture.prixDesignation }}</td>
            </tr>
            <tr>
                <th>Date_facture</th>
                <td>{{ facture.dateFacture ? facture.dateFacture|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ facture.montant }}</td>
            </tr>
        </tbody>
    </table>
  <div class="d-flex justify-content-center">
    <a href="{{ path('app_facture_index') }}">back to list</a>
    </div>
      <div class="d-flex justify-content-center">
    <a href="{{ path('app_facture_edit', {'id': facture.id}) }}">edit</a>
 </div>
   <div class="d-flex justify-content-center">
    <a href="{{ path('app_facture_envoyer') }}">envoyer</a>

 </div>
   <div class="d-flex justify-content-center">
    <a href="{{ path('facture_pdf', {'id': facture.id }) }}">pdf</a>
 </div>
   <div class="d-flex justify-content-center">
    <a href="{{ path('app_checkout') }}">checkout</a>
    </div>
   
<div class="d-flex justify-content-center">
    {{ include('facture/_delete_form.html.twig') }} 
    </div>
{% endblock %}
